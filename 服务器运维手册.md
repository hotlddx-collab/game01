# 怪物森林 · 服务器运维手册

后端部署在阿里云轻量应用服务器，通过 systemd 服务 `monster` 常驻运行。
本手册汇总所有常用运维指令。

---

## 基本信息

| 项目 | 值 |
|------|-----|
| 公网 IP | `106.14.139.241` |
| 端口 | `8765` |
| 登录用户 | `admin`（非 root，命令前加 `sudo`） |
| 项目目录 | `~/game01`（即 `/home/admin/game01`） |
| 后端目录 | `~/game01/agent_server` |
| systemd 服务名 | `monster` |
| Python 环境 | `~/game01/agent_server/.venv` |
| 客户端连接地址 | `ws://106.14.139.241:8765/ws?sid=<会话id>` |
| 健康检查 | `http://106.14.139.241:8765/` |

---

## 一、登录服务器

- 阿里云控制台 → 轻量应用服务器 → 远程连接（网页终端）；或
- 本机终端：`ssh admin@106.14.139.241`（首次会提示确认主机指纹，输 `yes`）。

---

## 二、启停与状态（systemd）

```bash
sudo systemctl start monster      # 开服
sudo systemctl stop monster       # 停服（不玩时）
sudo systemctl restart monster    # 重启（改代码/改配置后）
sudo systemctl status monster     # 查看运行状态
sudo systemctl enable monster     # 开机自启（已开，一般不用再执行）
sudo systemctl disable monster    # 取消开机自启
```

看到 `Active: active (running)` 即正常运行。

---

## 三、看日志

```bash
journalctl -u monster -f          # 实时日志（Ctrl+C 退出）
journalctl -u monster -n 100      # 最近 100 行
journalctl -u monster --since "10 min ago"   # 最近 10 分钟
```

排查"连不上/报错"时先看实时日志，观察是否有 client connected、LLM 错误等。

---

## 四、更新代码（发新版本）

在本机把改动 push 到仓库后，服务器执行：

```bash
cd ~/game01
git pull
sudo systemctl restart monster
sleep 2
systemctl status monster --no-pager | head -5
curl -s http://127.0.0.1:8765/ | head -c 200      # 应返回 {"status":"ok",...}
```

> 注意：改了客户端脚本（`scripts/`）后，玩家端要**重新导出**才生效；只改后端（`agent_server/`）则只需在服务器上 `git pull` + `restart`。

---

## 五、健康检查 / 连通性

```bash
# 服务器本机
curl -s http://127.0.0.1:8765/ | head -c 300

# 任意外网机器（验证公网可达）
curl -s http://106.14.139.241:8765/ | head -c 300
nc -vz 106.14.139.241 8765        # 测端口是否开放
```

返回含 6 个 NPC id 的 JSON = 正常。

---

## 六、删档 / 清库

**会话隔离后，每个玩家的存档是独立的 `data/{sid}.db` 文件。**

### 清空所有玩家存档（不可逆！）
```bash
sudo systemctl stop monster
rm -f ~/game01/agent_server/data/*.db  ~/game01/agent_server/data/*.db-*
rm -f ~/game01/agent_server/town.db    ~/game01/agent_server/town.db-*
sudo systemctl start monster
```
重启后自动重建空表，所有人从头开始。

### 只清某一个玩家
```bash
ls ~/game01/agent_server/data/          # 先看有哪些会话库
rm -f ~/game01/agent_server/data/<那个sid>.db*
```
删单个库不影响其他玩家，无需停服（该玩家重连即建新空库）。

### 查看有哪些玩家世界
```bash
ls -lh ~/game01/agent_server/data/
```

---

## 七、更换 DeepSeek API Key

Key 存在服务器的 `.env`（不在 git 里）。改法：

```bash
cd ~/game01/agent_server
nano .env                          # 修改 DEEPSEEK_API_KEY=sk-xxxx，Ctrl+O 保存 Ctrl+X 退出
sudo systemctl restart monster     # 重启生效
```

> 本机的 `agent_server/.env` 只在编辑器里跑本地后端时用，和线上互不影响。

---

## 八、防火墙 / 端口

端口 `8765` 已在阿里云"防火墙"放行 TCP。若换端口或重开：
- 阿里云控制台 → 轻量服务器 → 防火墙 → 添加规则 → TCP + 端口号。
- 服务器监听端口由 `.env` 的 `PORT` 控制（默认 8765）。

---

## 九、关于费用

- 轻量应用服务器是**包月/包年**计费，`stop monster` 只停进程，**不影响费用**。
- 真正省钱只能到期不续或退订；停机一般仍计费。
- 不玩时停服的意义：省内存、避免意外触发 LLM 调用产生 API 费用。

---

## 常用速查

```bash
# 一键状态 + 健康检查
systemctl status monster --no-pager | head -5 && curl -s http://127.0.0.1:8765/ | head -c 200

# 更新并重启
cd ~/game01 && git pull && sudo systemctl restart monster

# 看实时日志
journalctl -u monster -f
```
