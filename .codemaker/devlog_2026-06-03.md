# 开发日志 2026-06-02 ~ 06-03

## 概览

NPC 行为系统大改造：寻路 + 对话感知 + 日程丰富。本阶段结束，下阶段进入"背景生物 / 季节天气 / 传闻池"等内容扩展。

---

## 6/2 — NPC 寻路系统重写

### NavRegion 切换到 Godot 4.6 新 API

**问题**：旧 `make_polygons_from_outlines()` 报 "Convex partition failed"
- rock 层 1615 个 tile 加 4px margin → 相邻矩形重叠
- 外边界与孔洞绕向不一致
- 反复尝试无果

**解法**：彻底改用 `NavigationServer2D.bake_from_source_geometry_data()`
- `parsed_geometry_type = PARSED_GEOMETRY_STATIC_COLLIDERS`
- 自动从场景内 StaticBody2D 烘焙，无需手写 polygon
- 0 错误，0 警告

**回调修复**：lambda 里 `await` 不工作 → 改成命名方法 `_on_bake_done` + `call_deferred("_notify_npcs")`

### NPC 不动的根因

经过多轮调试，根因是 **WANDERING 状态还在用废弃的 `_nav_agent.velocity`**（设置后等 `velocity_computed` 回调），但回调被改成 `pass` 了。**速度被设但永远不会触发移动**。

修：TRAVELING + WANDERING 都直接 `velocity = ... ; move_and_slide()`。

### lack/rock 碰撞修复

- 用户手画的 lack/rock 节点没挂 ObstacleLayer 脚本 → 只有视觉没碰撞
- 自动给两个节点挂上脚本 + 推荐参数（lack 全格阻挡 / rock 逐格 30%-70%）
- ObstacleLayer 加 `merge_cells` 选项区分两种用法

### 路网坐标对齐

- `home_cui` 被用户拖到 x=-103（出地图），自动修回 (270, 530)
- 所有 home_X 节点对齐真实建筑入口（差距 ≤16px）
- 补充 bakery_door / post_door / plaza_center 节点
- 测试：`home_bear → home_fox` 路径 7 跳全经过路网节点

### 自动测试扩展（46 项）

`tests/test_npc_behavior.py` 新增两类：
- **路径沿主路验证**：A* 路径中间节点必须经过 wp_X（横向主路）
- **碰撞层配置验证**：脚本挂载 + tile 数据 + 参数合理（y_frac+h_frac≤1）

---

## 6/3 — NPC 对话感知 + 日程丰富 + 寻路完善

### NPC 对话地图感知（地点+附近NPC）

**新增 `data/world/location_descriptions.json`**：12 个地点氛围描述
- 每个地点第一人称感受："村东的湖边，听得见水声..."

**`agent_server/personas.py`** 加 `get_location_description(loc_id)` 加载缓存

**`agent_server/agent.py`** prompt 模板加 `{location_block}` 字段
- 注入 `【这个地方】描述`
- 注入 `【此刻附近还有】NPC 名字列表`（客户端发 `nearby_npcs` 字段）

**`scripts/animal.gd`** `get_current_context()` 加 `nearby_npcs`
- `_scan_nearby_npc_names(120.0)` 扫描 120px 范围内同 group NPC

**坑**：第一次实现用了 `from . import personas` 相对导入 → LLM 报错 NPC 不说话
- 修：改用绝对导入 `from personas import get_location_description`（与项目风格一致）

### NPC 日程丰富化（方案 B）

**6 个 NPC 全部重写 schedule**：每人 10-12 段工作日 + 6-9 段周末
- 苔老板：04:30 揉面 → 06:00 烤第一炉 → ... → 22:30 睡觉
- 焰仔：09:00 顺路买面包跟苔老板碰面 → 11:00 给老咸送漂流瓶信
- 小翠：06:30 采新鲜草药 → 17:00 给苔老板送薄荷
- 老咸 + 煊赫：13:30 广场并肩坐着不说话也不走
- 等等

**周末识别**：`game_day % 7 in [5, 6]` → 用 `schedule_weekend`
- 工作日忙业务，周末偏自然区域（河、森林、高处）

**碰面节点设计**：
- 焰仔 ↔ 苔老板（09:00 早饭）
- 焰仔 ↔ 老咸（11:00 漂流瓶）
- 焰仔 ↔ 小翠（12:00 草药包）
- 焰仔 ↔ 小蓝（15:00 外地趣事）
- 小翠 ↔ 苔老板（17:00 薄荷）
- 老咸 ↔ 煊赫（13:30 + 15:30 广场）

### NPC 卡屋顶问题（最终修复）

**问题**：NPC 沿主路到 (400, 350)，要去 bakery_door (400, 525)，直线南下被建筑挡住，卡在屋顶滑动

**根因**：建筑没碰撞 → NPC 穿过建筑 / 或建筑有碰撞 → NPC 撞墙不绕

**修复**：
1. **Building 自动碰撞**：building.gd `_update_collision()` 自动加 StaticBody2D（下半 90%×60%）
2. **NPC 寻路改回 NavAgent**：TRAVELING / WANDERING 优先用 `_nav_agent.get_next_path_position()`，自动绕建筑
3. **降级保护**：NavAgent 返回当前位置时回退直线
4. **卡死检测**：2 秒没移动 4 像素 → 跳过当前路径点

NavAgent 现在终于能真正发挥作用：navmesh 已包含所有 StaticBody2D（lack 湖、rock 石、buildings），自动绕过。

---

## 当前指标

- 自动测试：46/46 全绿
- Godot 启动：0 ERROR / 0 WARNING
- NPC 行为：6 人 × (11 工作日 + 8 周末) ≈ 114 段/周
- 路网：18 节点 / 17 连线
- 地图碰撞：lack 64 体 + rock 1615 体 + buildings 9 体（自动）

---

## 下阶段规划候选

| 方向 | 收益 | 工作量 |
|------|------|--------|
| **背景生物**（鸡/蝴蝶/青蛙/猫/鱼） | 世界活起来 | 中 |
| **季节/天气系统** | 注入对话，氛围多样化 | 中 |
| **传闻池** (世界共享 lore) | NPC 对话深度提升 | 小-中 |
| **NPC 关系网升级** | 涌现剧情更复杂 | 中 |

下次会话讨论选哪个。

---

## Commits（按时间倒序）

```
9cf377d  fix: NPC 移动优先用 NavAgent 路径绕过建筑 + 卡死检测
5284f9d  fix: 修 LLM 模块导入错误 + Building 自动碰撞
4d5011a  feat: NPC 日程丰富化 - 工作日+周末
7a8a077  feat: NPC对话注入地图感知 - 地点描述 + 附近NPC
c261fb9  fix: 目标直接用路网入口 + 缩小 wander_radius
45566b0  fix: WANDERING 直接 move_and_slide（不动根因）
8e39a90  fix: NPC 移动改回直接 move_and_slide
4c3bf17  fix: animal.gd 语法错误
ad187bb  feat: Animal @tool 圆圈出生点（已撤销）
6e5db62  chore: 删除未生效的 @tool 圆圈代码
ef0b3c5  chore: NPC 节点名改中文
4441455  fix: 小蓝旅居样式
8be2654  feat: 新增小翠草药屋 + 小蓝旅居
```
