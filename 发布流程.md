# 怪物森林 · 发布流程

每次「更新日志 + 上传 GitHub + 发布 Win/Mac + 更新云服务器」照此执行。
服务器运维细节见 `服务器运维手册.md`。

---

## 关键路径速查

| 项 | 值 |
|---|---|
| 项目根目录 | `/Users/lddx/Documents/GitHub/game01` |
| Godot 可执行文件 | `/Applications/Godot_mono.app/Contents/MacOS/Godot` ← **不是** `Godot.app` |
| Git 远程 | `origin` → `https://github.com/hotlddx-collab/game01.git`，分支 `main` |
| 导出预设 | `Windows Desktop` / `macOS`（见 `export_presets.cfg`） |
| Windows 产物 | `build/怪物森林.exe`（内嵌 pck，约 176M） |
| macOS 产物 | `build/怪物森林.app`（约 258M，导出时自动签名） |
| 云服务器 | `admin@106.14.139.241`，项目 `~/game01`，服务 `monster` |
| 健康检查 | `http://106.14.139.241:8765/` |

---

## 第 1 步 · 更新开发日志

编辑 `docs/开发日志.md`，在文件顶部（`# 开发日志` 之下）插入新一节：

```markdown
## YYYY-MM-DD · 本轮主题

### 功能名
- 改了什么、为什么

### Bug 修复：现象一句话
- 根因：……
- 修复：……

### 待办（下次）
- ……
```

写作要点：
- **写根因，不只写现象**。日后重开对话靠这个恢复上下文。
- 新增/修改的文件路径要点名（如 `agent_server/quiz.py`）。
- 「待办（下次）」承接上一节未做完的条目，别丢。

---

## 第 2 步 · 提交并推送 GitHub

```bash
cd /Users/lddx/Documents/GitHub/game01
git status --short          # 先看改了啥
git add -A
git commit -F - <<'EOF'
feat: 一句话概括本轮

- 模块: 做了什么
- 模块: 做了什么
EOF
git push origin main
```

---

## 第 3 步 · 导出 Windows 版

```bash
cd /Users/lddx/Documents/GitHub/game01
mkdir -p build
/Applications/Godot_mono.app/Contents/MacOS/Godot \
  --headless --export-release "Windows Desktop"
ls -lh build/怪物森林.exe
```

输出末尾出现 `[ DONE ] savepack` 即成功。

> 两个平台的产物都落在 `build/`（已在 `.gitignore`，不会进仓库），别再往根目录导。

---

## 第 4 步 · 导出 macOS 版

```bash
cd /Users/lddx/Documents/GitHub/game01
/Applications/Godot_mono.app/Contents/MacOS/Godot \
  --headless --export-release "macOS"
du -sh build/怪物森林.app
```

末尾出现 `正在对捆绑包进行代码签名` + `[ DONE ] export` 即成功。

> 顺带可用 `--headless --import` 做 GDScript 语法核对，有语法错会在导入阶段报出来。

---

## 第 5 步 · 更新云服务器

```bash
ssh admin@106.14.139.241
cd ~/game01
git pull
sudo systemctl restart monster
sleep 2
systemctl status monster --no-pager | head -5
curl -s http://127.0.0.1:8765/ | head -c 200
```

判断标准：
- `Active: active (running)`
- 健康检查返回含 6 个 NPC id 的 JSON

**⚠️ 已知问题：本机 SSH 进不去。**
服务器只允许公钥登录，本机没配对应私钥，直接 `ssh` 会返回
`Permission denied (publickey)`。两条出路：

1. **阿里云控制台 → 轻量应用服务器 → 远程连接（网页终端）**，手工粘上面的命令；
2. 本机配好私钥后再让 AI 代跑：
   ```bash
   ssh-copy-id -i ~/.ssh/你的私钥.pub admin@106.14.139.241
   ```

外网侧验证（本机就能跑，不需登录）：

```bash
curl -s -m 10 http://106.14.139.241:8765/ | head -c 300
```

---

## 什么时候必须做哪一步

| 改动范围 | 需要重启服务器 | 需要重新导出客户端 |
|---|:---:|:---:|
| 只改 `agent_server/` | ✅ | ❌ |
| 只改 `scripts/` `scenes/` | ❌ | ✅ |
| 改 `data/` 下 JSON | ✅（后端读） | ✅（前端也打包了） |
| 新增数据库表（`db.py`） | ✅ 重启时自动建表 | ❌ |

---

## 常见坑

- **窗口一屏放不下**：`project.godot` 的 `window_width_override` / `window_height_override` 是开发期在大屏预览用的，一旦留在发布版会强制按该尺寸开窗（曾设成 2560×1440，1080p 屏根本放不下）。发布前确认 `[display]` 段只有 `viewport_width/height`，没有 override。
- **Godot 路径写错**：必须是 `Godot_mono.app`，写成 `Godot.app` 会「文件不存在」。
- **导出前先 push**：服务器靠 `git pull` 取代码，没推上去等于没更新。
- **只重启不 pull**：跑的还是旧代码，白重启。
- **新表没生效**：`db.py` 的 `SCHEMA` 在服务启动时建表，改完必须 restart。
- **玩家还是旧版**：客户端脚本改动只有重新导出的包才带上，线上重启救不了。
